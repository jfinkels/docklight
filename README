This program computes statistics from data read from a Docklight log file.

To run this program, do

  ./docklight.py </path/to/inputfile>

where </path/to/inputfile> is the name of the file containing the Docklight log
data.

For example, run the example by executing the following command

  ./docklight.py example/example_log_file.txt
